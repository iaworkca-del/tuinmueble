# === Mi Propiedad — Variables de Entorno ===
# Copia este archivo como .env y completa los valores reales.
# NUNCA subas el .env a git.

# --- IA (Anthropic Claude) ---
ANTHROPIC_API_KEY=sk-ant-api03-TU_CLAVE_AQUI

# --- Sesiones (generar con: python -c "import secrets; print(secrets.token_hex(32))") ---
SESSION_SECRET_KEY=cambia-esto-por-una-cadena-aleatoria-larga

# --- Administrador inicial ---
ADMIN_USUARIO=admin
ADMIN_PASSWORD=tu_password_seguro

# --- Instagram via Upload Post (opcional) ---
UPLOADPOST_API_KEY=
UPLOADPOST_USER=

# --- Modo de imagen (web o local) ---
# IMAGE_MODE=web

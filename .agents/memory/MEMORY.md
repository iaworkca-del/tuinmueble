# Memory Index

- [Passlib + bcrypt incompatibility](passlib-bcrypt-issue.md) — passlib's bcrypt backend can crash on startup with newer bcrypt; use the `bcrypt` package directly instead.
- [Gemini free-tier image quota](gemini-image-quota.md) — free API keys have a 0 daily quota for image-generation models; always isolate image failures so text generation still succeeds.
